/** <summary>
/*  Specifies precision of date data type, i.e. if date contains year, month and day or only year and month.
/*  </summary>
**/

export enum DateTypePrecision { // This enum must match enum XmlDateTypePrecision
  YearMonthDay = 0,
  YearMonth = 1,
}
